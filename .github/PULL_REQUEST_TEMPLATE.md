## What this changes

<!-- One or two sentences describing the change. If it fixes an open issue, link it. -->

## How I tested

<!-- e.g. "ran `claude --plugin-dir ./` and verified `/plugin install` still works", "validated manifests against upstream schemas", "n/a — README change only". -->

## Checklist

- [ ] Manifest changes still validate (CI runs `check-jsonschema` against the upstream schemas; you can run the same locally).
- [ ] README install / usage instructions match what you've actually tested.
- [ ] If this changes a tool name, argument, or output shape, the production MCP server at `https://mcp.steep.app/mcp` already reflects it (this repo is metadata-only, not server code).

Thanks for contributing.
